/** The same chip a step down, for the session strip under the input: that row
 *  describes the run rather than composing a message, so it reads quieter. */
export const chipTriggerSm =
  "flex items-center gap-1.5 rounded-md px-2 py-1 text-xs font-medium text-foreground outline-none transition-colors hover:bg-accent hover:text-primary focus-visible:ring-1 focus-visible:ring-ring";

/** Trigger styling shared by the access/mode chips — matches ModelPicker. */
export const chipTrigger =
  "flex items-center gap-2 rounded-lg px-2.5 py-1.5 text-sm font-medium text-foreground outline-none transition-colors hover:bg-accent hover:text-primary focus-visible:ring-1 focus-visible:ring-ring";
