{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "preview-webview",
  "description": "The native preview surface reports its console back to the app. Loopback dev servers only — the spike flag keeps this off unless asked for.",
  "webviews": ["preview"],
  "remote": {
    "urls": ["http://localhost:*", "http://127.0.0.1:*"]
  },
  "permissions": ["core:event:default"]
}
